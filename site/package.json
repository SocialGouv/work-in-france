{
  "name": "work-in-france",
  "version": "0.0.1",
  "description": "La plateforme de demande d'autorisation provisoires de travail.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SocialGouv/work-in-france.git"
  },
  "license": "Apache-2.0",
  "scripts": {
    "css-watcher": "postcss source/static/css/styles.css --use postcss-import --use postcss-cssnext --output source/static/css/bundle.css --no-map --watch --verbose",
    "deploy": "clay build && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "postcss-cli": "^5.0.0",
    "postcss-cssnext": "^3.1.0",
    "postcss-import": "^11.0.0"
  }
}
