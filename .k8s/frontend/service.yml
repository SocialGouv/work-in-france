---
kind: Service
apiVersion: v1
metadata:
  name: ${K8S_NAMESPACE}-frontend-${BRANCH_HASH}
  labels:
    app: ${K8S_NAMESPACE}-frontend-${BRANCH_HASH}
    branch: ${BRANCH_NAME}
    commit: ${COMMIT}
    job: "${JOB_ID}"
    tag: "${COMMIT_TAG}"
spec:
  selector:
    app: ${K8S_NAMESPACE}-frontend-${BRANCH_HASH}
  ports:
    - port: ${PORT}
      name: frontend
